rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORÁRIO: Permitir acesso público para debug e desenvolvimento
    match /{document=**} {
      allow read, write: if true;
    }
  }
}